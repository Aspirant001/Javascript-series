"use strict"; // this uses all JS code as newer version

// alert("hello") // this is used in browser console

let name = "Shashwat"
let age = 21
let isLoggedIn = true

// number datatype (range = 2^53)
// bigint(same as long in java)
// string datatype("")
// boolean datatype
// null = this itself is a standalone value/ representation of a empty value
// undefined = value not defined after decalring a datatype
// symbol (used mostly in react, figma, etc) to define individual component or uniqueness
//object 

console.log(typeof null); //null is an object in typeof(sort of language error)
